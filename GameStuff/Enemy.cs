using UnityEngine;
using System.Collections;

public class Enemy : MonoBehaviour {
    //uploads image of enemy.  Removes it when P is pressed

    // Use this for initialization
    
    void Start()
    {
       
    }

    void onGUI()
    {
        //upload image of enemy. use spawn animation
    }
    // Update is called once per frame
    void Update()
    {
        //if(Input.GetKeyDown(KeyCode.P))
        //remove the image of enemy
    }
}

